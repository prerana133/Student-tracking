// src/context/authContext.js
import { createContext } from "react";

// Export the context object separately from the provider component
export const AuthContext = createContext(null);

export default AuthContext;
